query emailContent($locale: String) {
  emailCollection(limit: 1, locale: $locale) {
    items {
      preview
      websiteUrl
      logo {
        url
        description
      }
      greeting
      description
      buttonLabel
      linkDescription
      ignoreDisclaimer
      closingRemark
      signature
    }
  }
}
